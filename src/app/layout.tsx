import "~/styles/globals.css";

import { GeistSans } from "geist/font/sans";
import { type Metadata } from "next";

import { TRPCReactProvider } from "~/trpc/react";

export const metadata: Metadata = {
    title: "@jmnuf",
    description: "Generated by create-t3-app",
    icons: [{ rel: "icon", url: "/favicon.ico" }],
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
    const foycon_classes = "col-span-1 h-full bg-purple-600 border-[6px] border-black";
    return (
	<html lang="en" className={`${GeistSans.variable}`}>
	    <body>
		<TRPCReactProvider>
		    <div className="w-full h-[100vh] bg-slate-300 flex justify-center items-center">
			<div className="h-1/2 max-h-[60%] min-w-[720px] w-3/4 grid grid-cols-8 gap-0">
			    {/* foycon left */}
			    <div className={`${foycon_classes} animate-slideInN200 rounded-l-[25px] border-r`}></div>
			    {/* -- Screen -- */}
			    <div className="col-span-6 h-full border-4 border-black">
				<div className="w-full h-full flex border-8 border-slate-600 transition-[background-color] duration-[1s] delay-150">
				    <div className="h-full w-full flex p-1 opacity-0 animate-[fadeIn_500ms_ease-in_1s_forwards]">
					{children}
				    </div>
				</div>
			    </div>
			    {/* foycon right */}
			    <div className={`${foycon_classes} animate-[slideInN200_1.25s_ease-in-out_forwards] rounded-r-[25px] border-l delay-[1.5s]`}></div>
			</div>
		    </div>
		</TRPCReactProvider>
		<footer className="flex justify-center items-center absolute pt-2 pb-1 px-4 left-0 bottom-0 w-full bg-slate-600 text-slate-100"><p>{"Uicons by "}<a href="https://www.flaticon.com/uicons">Flaticon</a></p></footer>
	    </body>
	</html>
    );
}
